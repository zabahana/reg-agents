# Core (CPU or GPU)
torch>=2.1
onnx>=1.15

# Optional: custom CUDA fused bias+ReLU from Python (path A)
# Install the CuPy build that matches the VM CUDA major version, e.g.:
#   cupy-cuda12x
# numba

# TensorRT: prefer NGC container `nvcr.io/nvidia/tensorrt:*`
# rather than pip on arbitrary hosts.
