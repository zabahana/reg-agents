# --- Core runtime ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- LLM / embeddings (NVIDIA NIM is OpenAI-API compatible, so one client covers both) ---
openai==1.57.0

# --- Model Context Protocol (MCP) tool servers + clients (FastMCP + SSE) ---
mcp>=1.9.0,<2.0.0

# --- Retrieval / vector search (local dev). On GPU we swap FAISS for Milvus+cuVS. ---
faiss-cpu==1.9.0
numpy==1.26.4

# --- Model development bake-off (first-line training + champion selection). ---
# On GPU these map to RAPIDS cuML / XGBoost; the API is intentionally similar.
scikit-learn==1.5.2
pandas==2.2.3
xgboost==2.1.1

# --- Observability: Prometheus metrics for the A2A agents (scraped by
# kube-prometheus-stack; visualized in Grafana). ---
prometheus-fastapi-instrumentator==7.0.0

# --- Observability: OpenTelemetry distributed tracing across A2A hops
# (exported to Jaeger via OTLP). Same span model AIQ / NeMo Agent Toolkit emits.
# Active only when OTEL_EXPORTER_OTLP_ENDPOINT is set (monitoring profile). ---
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-httpx==0.48b0

# --- Demo UI ---
streamlit==1.40.1

# --- Dev / test ---
pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.8.3
