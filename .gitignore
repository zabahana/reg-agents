# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.local.yaml
k8s/secret.yaml

# Data artifacts
*.faiss
*.index
data/**/*.pkl

# Triton model files are build artifacts (generated by scripts/export_triton_model.py)
triton/model_repository/**/*.json
triton/model_repository/**/*.onnx
triton/model_repository/**/*.bin

# Local run artifacts + uv-managed toolchain (kept inside workspace)
.run/
.tools/
.pythons/
.uvcache/
uv.lock

# OS
.DS_Store
# Batch-scoring outputs are run artifacts (keep the committed sample only)
data/scoring/scored_*.csv
data/complaints/raw_cfpb_dump.json
