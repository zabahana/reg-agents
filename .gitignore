# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.local.yaml
k8s/secret.yaml

# Data artifacts
*.faiss
*.index
data/**/*.pkl

# Triton model files are build artifacts (export_triton_model /
# export_complaint_triton_model)
triton/model_repository/**/*.json
triton/model_repository/**/*.onnx
triton/model_repository/**/*.bin
triton/model_repository/**/*.joblib

# HITL audit log (local analyst dispositions)
data/hitl/*.jsonl

# Local run artifacts + uv-managed toolchain (kept inside workspace)
.run/
.tools/
.pythons/
.uvcache/
uv.lock

# OS
.DS_Store
# Batch-scoring outputs are run artifacts (keep the committed sample only)
data/scoring/scored_*.csv
data/complaints/raw_cfpb_dump.json

# DPO policy weights (regenerate with scripts/train_dpo_from_judges.py)
docs/complaint_model/artifacts/dpo_policy/
docs/complaint_model/judge_agreement_rows.jsonl
docs/complaint_model/dpo_panel_*_rows.jsonl
