# Internal Audit Report — Consumer Credit Default (PD) Scorecard

*Third line of defense — Audit Agent (process assurance)*

> Generated by the `reg-agents` model-development lifecycle on 2026-07-10 20:08 UTC.
> LLM: **nim** (`meta/llama-3.1-8b-instruct`) · Bake-off: **scikit-learn** (GPU analog: RAPIDS cuML / XGBoost).
> Task: **Consumer Credit Default (PD) Scorecard** · Champion: **logistic_regression** (ROC-AUC 0.9049, KS 0.7152).

---

**AUDIT REPORT**

**Objective & Scope**

The objective of this audit is to assess the effectiveness of the model risk management process for the credit default (PD) scorecard model. The scope of this audit includes the model development document, independent validation report, and governance regulations.

**Process Walkthrough (development → independent validation → approval)**

The development team developed the credit default (PD) scorecard model using a dataset containing 1200 samples. The model was designed to estimate the probability of default (PD) for consumer loan applications, supporting credit decisioning under ECOA/Reg B and FCRA fair-lending constraints.

The development team selected the `logistic_regression` model as the champion based on its highest ROC-AUC (0.9049) and KS (0.7152) scores, as well as its PR-AUC (0.6442) score, on a stratified 30% hold-out. However, the next-best model, `random_forest`, had a lower ROC-AUC (0.8939) but higher PR-AUC (0.6065) and KS (0.7186) scores.

The independent validation report reviewed the model development document and considered the candidate leaderboard and relevant regulations. The report found that the champion model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.

The development team has considered fair-lending review at development, validation, and ongoing monitoring. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.

**Control Assessment**

The following controls were assessed:

1.  **Independence of Validation from Development**: The independent validation report reviewed the model development document and considered the candidate leaderboard and relevant regulations. However, the report found that the champion model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.
2.  **Documentation Completeness**: The model development document and independent validation report provide sufficient information for a fair-lending/consumer-protection review. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.
3.  **Evidence of Effective Challenge**: The development team has considered fair-lending review at development, validation, and ongoing monitoring. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.
4.  **Issue Tracking**: The development team has considered fair-lending review at development, validation, and ongoing monitoring. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.
5.  **Approval Before Deployment**: The development team has considered fair-lending review at development, validation, and ongoing monitoring. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.

**Audit Findings & Issues (rated High/Medium/Low with owners)**

1.  **Severity: Medium**
    *   The champion model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.
    *   The model's recall (0.6667) and precision (0.5833) scores suggest that the model may not be optimal for all metrics.
    *   **Owner:** Development Team
2.  **Severity: Low**
    *   The model development document provides a clear overview of the model's purpose, development process, and intended use.
    *   The model is designed to support credit decisioning under ECOA/Reg B and FCRA fair-lending constraints.
    *   **Owner:** Development Team
3.  **Severity: Medium**
    *   The development team has considered fair-lending review at development, validation, and ongoing monitoring.
    *   However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.
    *   **Owner:** Development Team

**AUDIT OPINION**

**Satisfactory**

The model risk management process for the credit default (PD) scorecard model is satisfactory. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.

The development team has considered fair-lending review at development, validation, and ongoing monitoring. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.

The development team should implement the remediations outlined above to address these concerns.

**Justification**

The model risk management process for the credit default (PD) scorecard model is satisfactory. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.

The development team has considered fair-lending review at development, validation, and ongoing monitoring. However, the model's performance metrics suggest that the model may not be optimal for all metrics, particularly PR-AUC and recall.

The development team should implement the remediations outlined above to address these concerns.

**Recommendations**

1.  **Retain the Challenger Model**: Retain the challenger model, `random_forest`, for ongoing benchmarking (effective challenge) to evaluate the champion model's performance and potential improvements.
2.  **Conduct Further Analysis**: Conduct further analysis to assess the model's fairness and bias, including disparate impact analysis for models.
3.  **Implement Additional Monitoring Activities**: Implement additional monitoring activities to ensure the ongoing performance and validity of the model.

By implementing these recommendations, the development team can ensure that the model is fair, unbiased, and optimal for all metrics.
